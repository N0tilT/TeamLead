[project]
name = "server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiohttp>=3.13.5",
    "dotenv>=0.9.9",
    "loguru>=0.7.3",
    "pydantic>=2.13.3",
    "yandex-tracker-client>=2.10",
]
